% !TeX root = ../main.tex
% Add the above to each chapter to make compiling the PDF easier in some editors.

\chapter{Methodology}\label{chapter:methodology}

\section{Problem Statement and Prediction Target}

In this thesis, we address the transfer of the T3 runtime prediction approach from Umbra to PostgreSQL parsed plans.  
The central methodological goal is to preserve T3’s core modeling principles.
Upon that, we adapt plan conversion and feature extraction to PostgreSQL-specific plan information.

Also, the prediction target is total query runtime.  
Following T3, each query plan is decomposed into pipelines and a per-tuple runtime is predicted for each pipeline.
Afterwards, we aggregate the per-tuple and then the pipeline runtimes to obtain the final query-runtime estimate.

\section{Data and Preprocessing}
Regarding the training data, we use the public Zero-Shot benchmark data introduced with the Zero-Shot Cost Model.  
The benchmark is particularly suitable for this thesis because it consists of real-world datasets (e.g., IMDB) and established benchmark workloads (e.g., SSB, TPC-H and JOB).
Overall, the benchmark contains 20 databases with large variation in tables, columns, and foreign-key relationships \parencite{zeroshot}.

Having this multitude of databases allows us to test the generalization capabilities of our model.
Similar to the T3 paper, we can train the model on all but one databases and test on the remaining one.
Moreover, as the widely used JOB workload is included, we can compare our performance predictions to other implementations later on.
Further, PostgreSQL is the intended database system of our implementation, which avoids the effort of building a separate PostgreSQL benchmark.

Following the benchmark construction described in the paper, queries are generated per database and executed on a PostgreSQL DBMS (v12) on c8220 nodes on the CloudLab platform.
Then, they are stored as raw traces (i. e., the output of \texttt{EXPLAIN ANALYZE}).
Next, the authors preprocess the queries to remove very long-running queries (queries above 30\,s are excluded) to keep the benchmark practical and reproducible \parencite{zeroshot}.
A further preprocessing step is to parse the raw traces into structured plan representations.
These parsed plans (15000 per database) contain the operator nodes found in the raw traces, but organize them in a tree structure with node-wise attributes.
For every node, e.g., operator type, estimated/actual cardinalities, widths, and runtimes are stored.
The tree structure can be used to create pipelines and the node information will be used to train the model.
In the upcoming sections, we will describe these key implementation details.

Consequently, the parsed plans offer a good starting point for our pipeline and we can begin without further preprocessing.



\section{Implementation}

\subsection{Components Taken from T3}

T3 is our source model and we transfer its core prediction logic from Umbra to PostgreSQL parsed plans.  
Hence, we adopt its three central design principles:
a pipeline-based query plan representation, tuple-centric prediction targets, and gradient-boosted decision trees for runtime prediction~\parencite{t3}.

We preserve T3's learning and aggregation mechanics: query runtime is obtained by summing predicted pipeline runtimes, each derived from a per-tuple prediction scaled to the pipeline input cardinality as in the original T3 setup.

Training follows the tuple-centric T3 recipe implemented in our codebase (e.g.\ \texttt{src/zeroshot/training\_zeroshot\_tpch\_holdout.py}): we build per-pipeline training rows, transform per-tuple targets with $-\log(t)$, and fit a LightGBM gradient-boosted tree model under a fixed protocol (objective, number of boosting rounds, train/validation split by query).

PostgreSQL parsed plans are first converted into a T3-compatible internal plan and pipeline layout in \texttt{src/zeroshot/zeroshot\_to\_t3.py} (operator mapping, pipeline breakers, node identifiers, and plan-level metadata).  
This step supplies the pipeline decomposition and operator graph that the rest of the T3-style pipeline still assumes; feature values themselves are filled by our PostgreSQL-specific extension described below.

For ablation only, we additionally evaluate a \emph{direct transfer} variant that keeps the original Umbra-oriented \texttt{FeatureMapper} (\texttt{src/features.py}) on the converted plans; we report this comparison in \autoref{subsec:eval_pg_vs_umbra_features}.

\subsection{Extensions for PostgreSQL Parsed Plans}

Our main system replaces Umbra-oriented pipeline features with a dedicated PostgreSQL encoding.  
During conversion, each plan node carries a \texttt{pg} copy of the Zero-Shot \texttt{plan\_parameters} payload.  
The \texttt{PgFeatureMapper} in \texttt{src/pg\_features.py} then builds one fixed-length feature vector per pipeline from those fields: aggregated cardinalities, planner cost and startup estimates, tuple width, planned worker counts, operator-type counts, simple statistics over scan filters, and an additional operator-level block that sums PG-native signals over operators in pipeline order.  
The mapper exposes the same interface as T3's feature mapper (\texttt{get\_pipeline\_estimation\_matrix}, \texttt{get\_pipeline\_scan\_sizes}) so the existing per-tuple runtime model and training loop stay unchanged.

Observed operator timings from the trace are not used as model inputs; they remain available only for defining training labels and pipeline durations where needed.

\section{Other Performance Prediction Implementations}

Add other implementations that you want to compare to.

\section{Training Setup}

Trees nennen
Add auch hier deine system specs, seed, split etc...

\section{Section}
Citation test~\parencite{latex}.

Acronyms must be added in \texttt{main.tex} and are referenced using macros. The first occurrence is automatically replaced with the long version of the acronym, while all subsequent usages use the abbreviation.

E.g. \texttt{\textbackslash ac\{TUM\}, \textbackslash ac\{TUM\}} $\Rightarrow$ \ac{TUM}, \ac{TUM}

For more details, see the documentation of the \texttt{acronym} package\footnote{\url{https://ctan.org/pkg/acronym}}.
\subsection{Subsection}

See~\autoref{tab:sample}, \autoref{fig:sample-drawing}, \autoref{fig:sample-plot}, \autoref{fig:sample-listing}.

\begin{table}[htpb]
  \caption[Example table]{An example for a simple table.}\label{tab:sample}
  \centering
  \begin{tabular}{l l l l}
    \toprule
      A & B & C & D \\
    \midrule
      1 & 2 & 1 & 2 \\
      2 & 3 & 2 & 3 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htpb]
  \centering
  % This should probably go into a file in figures/
  \begin{tikzpicture}[node distance=3cm]
    \node (R0) {$R_1$};
    \node (R1) [right of=R0] {$R_2$};
    \node (R2) [below of=R1] {$R_4$};
    \node (R3) [below of=R0] {$R_3$};
    \node (R4) [right of=R1] {$R_5$};

    \path[every node]
      (R0) edge (R1)
      (R0) edge (R3)
      (R3) edge (R2)
      (R2) edge (R1)
      (R1) edge (R4);
  \end{tikzpicture}
  \caption[Example drawing]{An example for a simple drawing.}\label{fig:sample-drawing}
\end{figure}

\begin{figure}[htpb]
  \centering

  \pgfplotstableset{col sep=&, row sep=\\}
  % This should probably go into a file in data/
  \pgfplotstableread{
    a & b    \\
    1 & 1000 \\
    2 & 1500 \\
    3 & 1600 \\
  }\exampleA
  \pgfplotstableread{
    a & b    \\
    1 & 1200 \\
    2 & 800 \\
    3 & 1400 \\
  }\exampleB
  % This should probably go into a file in figures/
  \begin{tikzpicture}
    \begin{axis}[
        ymin=0,
        legend style={legend pos=south east},
        grid,
        thick,
        ylabel=Y,
        xlabel=X
      ]
      \addplot table[x=a, y=b]{\exampleA};
      \addlegendentry{Example A}
      \addplot table[x=a, y=b]{\exampleB};
      \addlegendentry{Example B}
    \end{axis}
  \end{tikzpicture}
  \caption[Example plot]{An example for a simple plot.}\label{fig:sample-plot}
\end{figure}

\begin{figure}[htpb]
  \centering
  \begin{tabular}{c}
  \begin{lstlisting}[language=SQL]
    SELECT * FROM tbl WHERE tbl.str = "str"
  \end{lstlisting}
  \end{tabular}
  \caption[Example listing]{An example for a source code listing.}\label{fig:sample-listing}
\end{figure}
